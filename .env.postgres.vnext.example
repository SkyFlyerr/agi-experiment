# ===================================================================
# PostgreSQL Configuration for Server Agent vNext
# ===================================================================
# Copy this file to .env.postgres.vnext
# Used by Docker Compose postgres service

# PostgreSQL superuser
POSTGRES_USER=server_agent
# PostgreSQL superuser password (change in production!)
POSTGRES_PASSWORD=your_secure_password_here
# Default database name
POSTGRES_DB=server_agent_vnext
# PostgreSQL port (internal to container)
POSTGRES_PORT=5432

# Optional: Additional configuration
# Increase max connections if needed
# POSTGRES_MAX_CONNECTIONS=200
